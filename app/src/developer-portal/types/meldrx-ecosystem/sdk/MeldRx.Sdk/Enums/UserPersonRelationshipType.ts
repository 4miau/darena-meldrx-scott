export enum UserPersonRelationshipType {
    Self,
    Parent,
    Child,
    Sibling,
    Spouse,
    Relative,
    Provider,
    Other
}

export enum PersonTypes {
    ExistingPerson,
    NewPerson
}
